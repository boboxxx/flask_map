










});    });        // 如果使用 Google Maps: google.maps.event.trigger(map, 'resize');        // 如果使用 Leaflet: map.invalidateSize();        // 调用相应的地图重绘方法    sidebar.addEventListener('transitionend', () => {    // 例如：let map = L.map('map-container'); 或 const map = new google.maps.Map(...);    // 根据实际地图对象或库(如 Leaflet、Google Maps)进行调用    const sidebar = document.getElementById('sidebar');document.addEventListener('DOMContentLoaded', () => {